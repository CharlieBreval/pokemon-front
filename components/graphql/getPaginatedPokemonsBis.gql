query paginatedPokemonsBis($offset: Int!, $limit: Int!) {
  paginatedPokemons(offset: $offset, limit: $limit)
    @connection(key: "thelistofpierre", filter: []) {
    id
    name
    cartouche
    type
  }
}
